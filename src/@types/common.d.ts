export type GrandPrix = {
  grand_prix: string;
};

export type Ranking = {
  grand_prix: string;
  racing_date: string;
  driver: string;
  team: string;
  laps: number;
  time_retire: string;
};

export type Option = {
  label: string;
  value: string;
};
